# messaging-patterns

> Work in progress — production-grade messaging patterns in .NET (transactional outbox, idempotent consumers, saga with compensation, retry + dead-lettering, end-to-end tracing). Built as the flagship artifact of module 2 of my Solution Architect track.
